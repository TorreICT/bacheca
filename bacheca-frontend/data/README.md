# Data

This folder is reserved for optional local/private dashboard data.

Birthdays are no longer loaded from JSON. They come from the Google Calendar feed when an event title matches `Compleanno di Nome Cognome`.
